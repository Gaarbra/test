import React from 'react';
import './hero.css';

  const Hero = () => {
    return (
      <section className="hero-section">
        <div class="hero-content">
            <h1 class="hero-title">Let's brew kindness, one cup at a time.</h1>
              <p class="hero-description">Experience the finest coffee blends crafted with passion.</p>
            <button class="hero-button">Get Started</button>
        </div>
      </section>
    );
  };

export default Hero;
